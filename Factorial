// Ex if you input number 5 the output will be 120
package main

import "fmt"

func main() {
	var i, n, factorial int
	fmt.Scan(&n)
	faktorial = n
	for i = n; i > 1; i-- {
		factorial = factorial * (i - 1)

	}
	fmt.Println(factorial)
}
